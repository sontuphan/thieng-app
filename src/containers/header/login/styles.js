export default theme => ({
  noWrap: {
    flexWrap: 'nowrap',
  },
  stretch: {
    flex: '1 1 auto',
  },
  action: {
    flexWrap: 'nowrap',
    padding: `${theme.spacing(1)}px ${theme.spacing(2)}px`,
  }
});